generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Video {
  id           String   @id @default(uuid())
  title        String
  description  String
  url          String
  cloudinaryId String
  createdAt    DateTime @default(now())

  @@map("videos")
}
